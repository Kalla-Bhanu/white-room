# Benchmark Fixtures

Fixtures are stored as one folder per task type:

```text
bench/fixtures/<task_type>/
  input.md
  rubric.md
```

`input.md` contains the work item to benchmark. `rubric.md` contains a fixed checklist for judging an output. WR-016 only lists and reads fixtures; scoring is added later.

# Fixture: Question Detection

Scan the text below and identify every question.

The loader should list fixtures only. Do not add scoring yet.
Should the README mention the folder format?
We also need to keep the benchmark phase offline.
Is the tasks file updated after each packet?

Return the questions only.
